[
  [
    "tune",
    "",
    "hq, uhq, ll, ull or lossless (default hq) - What the encoder is tuned for; the tab already sets lossless at quality 0",
    "Quality & Tuning",
    "Picks what NVENC is tuned for, which moves the rate control and the buffering underneath whatever preset is in use. hq is the ordinary offline choice and the default. uhq asks for more again, and it is one of the few things the HEVC encoder has that the H.264 one does not - ffmpeg's option list stops at lossless there and carries a fifth constant here. ll and ull trade compression for latency, cutting the encoder's reordering and buffering so a frame comes out almost as soon as it goes in - which is what a live stream wants and what a file does not.\n\nlossless encodes the source exactly. It produces very large files, and the encoder switches itself into constant-QP mode for it whatever rate control was asked for.\n\nThis is the one row that can name an option the tab sets itself: with the quality value at 0 the tab emits -tune lossless. The grid's rows go on the command line after the rate control, so a tune set here wins - which also makes hq the way to take an encode back off lossless without touching the quality box.",
    "hq|The default, and the right choice for encoding a file.\nuhq|Ultra high quality; more analysis again, and HEVC-only among these two.\nll|Low latency, for live streaming rather than files.\null|Ultra low latency; the strongest form and the largest quality cost.\nlossless|Mathematically lossless output, at an enormous bitrate."
  ],
  [
    "spatial-aq",
    "",
    "0 or 1 (default 0) - Adaptive quantisation within a frame; moves bits to the flat areas that band first",
    "Quality & Tuning",
    "Adaptive quantisation moves bits between blocks within a frame, taking them from busy areas where quantisation error hides and giving them to flat ones where banding and blocking are obvious. Without it, dark and smooth regions are where an encode visibly falls apart first.\n\nNVENC's version is a plain on/off rather than the choice of modes a software encoder offers, with aq-strength beside it deciding how far the quantiser may shift. It is off by default, which is a large part of why a hardware encode can look worse than a software one at the same bitrate.\n\nThere is no real speed cost - the analysis is work the hardware does anyway - so this is close to free quality on anything with skies, gradients, fades or large flat surfaces.",
    "1|On; what almost any offline encode wants.\n0|The default, off."
  ],
  [
    "temporal-aq",
    "",
    "0 or 1 (default 0) - Adaptive quantisation across frames; needs lookahead, and a GPU that implements it",
    "Quality & Tuning",
    "Where spatial AQ redistributes bits inside one frame, temporal AQ redistributes them between frames, spending more on the parts of the picture that later frames will keep referencing. It is the same idea as a software encoder's macroblock tree.\n\nIt needs lookahead frames to work with, so it does nothing unless rc-lookahead is also set. It is the one AQ setting the hardware can refuse outright: ffmpeg's own check prints \"Temporal AQ not supported\" and carries on without it, which older GPUs will do.\n\nWorth having on material with sustained motion over a background worth protecting. On a fast, cut-heavy source there is much less for it to find.",
    "1|On; pair it with rc-lookahead or it does nothing.\n0|The default, off."
  ],
  [
    "aq-strength",
    "",
    "1-15 (default 8) - How far spatial AQ may shift the quantiser; raise for flat sources, lower for grain",
    "Quality & Tuning",
    "How far adaptive quantisation is allowed to shift the quantiser between blocks. Higher values take more bits out of detailed areas and put more into flat ones. It does nothing at all unless spatial-aq is on - ffmpeg's own description of this option says so.\n\nRaise it when flat regions - skies, walls, gradients, fades to black - show banding or blocking. Lower it when the source is grainy or heavily textured, because there the bits are better left where the detail is, and a strong setting will smooth the grain the rest of your settings are trying to keep.\n\nThe scale runs 1 to 15 with 8 in the middle, so this is a coarser control than a software encoder's fractional strength and one step is a real change. 0 is not off - it is refused as out of range.",
    "8|The default, the middle of the range.\n5|Grainy or heavily textured sources, keeping bits on detail.\n12|Flat, clean sources such as animation that band easily.\n15|The maximum; strong redistribution, and visible smoothing of texture."
  ],
  [
    "tf_level",
    "",
    "-1 auto, 0 off or 4 on (default -1) - NVENC's temporal filter; needs bf 4 or more and a recent GPU",
    "Quality & Tuning",
    "NVENC's own temporal filter, which averages across neighbouring frames before encoding to take out noise that would otherwise be expensive to code. It is a hardware feature of the newer encoder generations, and ffmpeg prints \"Temporal filtering not supported by the device\" and carries on without it where the GPU has no such block.\n\nIt has a hard requirement beside it: ffmpeg refuses the combination unless there are at least four B-frames, saying \"Temporal filtering needs at least 4 B-Frames (-bf 4)\". So this row and the bf row are set together or not at all.\n\nOnly -1, 0 and 4 mean anything - those are the levels NVENC defines, and the encoder answers anything else with \"Invalid temporal filtering level\". ffmpeg's own range check on this option is much wider than that and will happily let a 2 or a 7 through to be refused later on.\n\nLike any denoiser it cannot tell sensor noise from fine texture, so on a grainy film scan it takes the grain with it.",
    "-1|The default; the encoder decides.\n4|Temporal filtering on, which needs bf set to 4 or more.\n0|Explicitly off, overriding whatever the preset would pick."
  ],
  [
    "lookahead_level",
    "",
    "-1 unset, 0-3, or auto (default -1) - How hard the lookahead analyses; needs rc-lookahead set first",
    "Quality & Tuning",
    "How much work the lookahead does with the frames it has been given. Higher levels analyse more and may place frame types and quantisers better, at a cost in encoder throughput - which is what ffmpeg's own description of the option says.\n\nIt is meaningless on its own: with rc-lookahead at 0 there are no lookahead frames to analyse. Set that row first.\n\nThe named auto constant is 15, and it is not the same thing as the default of -1. -1 leaves the field unset so the driver's own behaviour applies; auto asks the driver to pick a level. The GPU can also cap what it will take, which ffmpeg reports as \"Lookahead level not supported\" along with the maximum.\n\nMeasured against this build the option refuses anything above 15.",
    "-1|The default; the field is left unset.\nauto|Asks the driver to pick a level; the constant behind it is 15.\n1|A light analysis pass over the lookahead frames.\n3|The deepest defined level, and the slowest."
  ],
  [
    "qp_cb_offset",
    "",
    "-12 to 12 (default 0) - Shifts the Cb chroma quantiser from luma; negative spends more bits on colour",
    "Quality & Tuning",
    "Offsets the quantiser used for the Cb chroma plane from the one used for luma. Negative values quantise chroma more finely, spending more bits on colour; positive values coarsen it to spend more on brightness detail.\n\nNVENC splits this into two rows where a software encoder has one, so a shift meant for chroma as a whole means setting qp_cr_offset to match. Setting only one of the two tints where the error lands rather than balancing it, which is occasionally what you want and usually not.\n\nNegative values are worth trying on saturated, colour-critical material, and on animation where flat colour areas show chroma banding. Positive values are a way to buy luma detail on a tight bitrate.",
    "0|The default.\n-2|Extra bits on colour; useful for animation and saturated sources.\n-4|A strong chroma bias, for colour-critical work.\n2|Coarser chroma, buying luma detail at low bitrates."
  ],
  [
    "qp_cr_offset",
    "",
    "-12 to 12 (default 0) - Shifts the Cr chroma quantiser from luma; the red-difference half of qp_cb_offset",
    "Quality & Tuning",
    "The same control as qp_cb_offset applied to the Cr plane. Negative values quantise the red-difference chroma more finely and positive ones more coarsely.\n\nThe two are separate options, so a plain \"spend more on chroma\" means giving both the same value. Left unequal they weight one chroma plane against the other, which moves where colour error shows rather than how much of it there is.\n\nChroma is subsampled in the formats this encoder is usually given, so it starts from a quarter of the samples luma has - which is why a small negative offset here can be worth more than the same bits spent on luma detail.",
    "0|The default.\n-2|Matches a qp_cb_offset of -2 for an even chroma bias.\n-4|A strong chroma bias, for colour-critical work.\n2|Coarser chroma, buying luma detail at low bitrates."
  ],
  [
    "highbitdepth",
    "",
    "0 or 1 (default 0) - Encodes at ten bits from an eight-bit source, which bands far less",
    "Quality & Tuning",
    "Ten-bit encoding does not need a ten-bit source. The extra precision is used inside the encoder, where it carries less rounding error through prediction and the transform, and what comes out has visibly finer gradients - skies, fades to black, studio walls - than the same encode at eight bits. It usually compresses a shade better as well.\n\nThis is not the colour format, which the tab sets itself and which decides what the encoder is handed. This decides what the encoder writes. Choosing a ten-bit colour format makes the output ten-bit on its own, so this row is for the case where the source is eight-bit and you want ten-bit output anyway.\n\nSet the profile row to main10 alongside it, that being the profile a ten-bit HEVC stream belongs to. The GPU has to have a ten-bit encoder, and ffmpeg says \"10 bit encode not supported\" where it does not.\n\nThe compatibility cost is much smaller than the same move in H.264: main10 is what HDR and most streaming HEVC already use, so current hardware decodes it, where H.264's high10 was never played by anything.",
    "0|The default; the output bit depth follows the colour format.\n1|Ten-bit output from an eight-bit source; set profile:v main10 too."
  ],
  [
    "split_encode_mode",
    "",
    "auto, forced, 2, 3, 4 or disabled (default auto) - Splits a frame across several NVENC engines on the cards that have them",
    "Quality & Tuning",
    "The workstation and datacentre cards carry more than one NVENC engine. Split encoding cuts each frame into horizontal strips and gives them to different engines, so one stream is encoded by several at once - which is throughput a single engine cannot reach and most of the reason those cards have more than one.\n\nThe constants are worth reading carefully, because 0 is auto and not off: disabled is 15. auto leaves it to the preset and the tuning info, forced turns it on with the driver choosing how many strips, and 2, 3 and 4 force that many - each doing nothing unless the card actually has that many encoders.\n\nIt is not free. A strip boundary is a place the encoder cannot predict across, so the frame is coded in pieces that know nothing about each other, and the compression cost grows with the number of strips. On any single-NVENC card, which is most GeForce hardware, there is nothing to split across and the setting changes nothing either way.\n\nweighted_pred rules it out - ffmpeg carries the message \"Split encoding not supported with weighted prediction enabled\" - so those two rows are a choice between them. The option is HEVC-only here; the H.264 encoder does not offer it.",
    "auto|The default; the preset and the tuning info decide.\nforced|On, with the driver choosing how many strips.\n2|Two strips, on a card with at least two encoders.\ndisabled|Off for every configuration; the constant behind it is 15."
  ],
  [
    "rc",
    "",
    "constqp, vbr or cbr (default -1, whatever the preset implies) - Overrides the rate control mode the tab has already set",
    "Rate Control",
    "Which rate control mode NVENC runs. Left alone the value is -1, and the mode follows from the preset and from the bitrate or quality arguments the tab has already put on the command line.\n\nThat is the thing to be careful about here. The tab emits its own rate control - a bitrate with a minimum, maximum and buffer size for a bitrate target, or -b:v 0 -cq N for a quality target - and this row overrides the mode underneath it. Asking for constqp under a bitrate target, or cbr under a quality target, produces a command whose two halves disagree, and the encoder resolves that rather than refusing it.\n\nSet it when you know which mode you want and the preset is picking the other one. Leave it alone otherwise.\n\nOlder NVENC builds had vbr_hq, cbr_hq and cbr_ld_hq beside these three. Measured against this build they are gone - only constqp, vbr and cbr parse - and the quality half of what they used to mean now lives in the preset and multipass settings.",
    "vbr|Variable bitrate; the mode a quality or average-bitrate target wants.\ncbr|Constant bitrate, for streaming and fixed-rate delivery.\nconstqp|A fixed quantiser, ignoring any bitrate target."
  ],
  [
    "rc-lookahead",
    "",
    "0 or more frames (default 0, off) - Frames analysed ahead of the encode; off by default, and four other rows need it",
    "Rate Control",
    "How many frames ahead the encoder looks before committing to a frame type and a quantiser. A longer lookahead paces bits better across a scene and lets the encoder put its I-frames where the cuts actually are, at the cost of memory and of that many frames of latency.\n\nIt is off by default on NVENC, and that matters more than the number itself: temporal-aq, b_adapt, no-scenecut and lookahead_level all describe what the lookahead should do, and all of them are inert while there is no lookahead to do it. This row is what switches that whole group on.\n\nThe encoder also needs enough buffered frames to hold the window. ffmpeg says \"Lookahead not enabled. Increase buffer delay (-delay)\" where it cannot, and a GPU with no lookahead block at all gets \"RC lookahead not supported\".\n\nPast roughly the length of a typical shot there is nothing left for it to learn.",
    "0|The default; no lookahead, and the rows depending on it do nothing.\n32|A sound general setting, about a second at 30 fps.\n60|Longer scenes and offline encoding.\n250|Deep lookahead; memory-hungry with little more to show for it."
  ],
  [
    "multipass",
    "",
    "disabled, qres or fullres (default disabled) - NVENC's own two passes, inside a single ffmpeg pass",
    "Rate Control",
    "NVENC can run two passes over each frame inside one encode: a first that measures how difficult the frame is and a second that codes it with that knowledge. qres does the first pass at quarter resolution, fullres at full.\n\nThis is not ffmpeg's two-pass, which runs the whole file twice. Nothing here writes a stats file and the encode stays a single run, so it is the way to get better bit distribution out of a hardware encoder without doubling the wall clock.\n\nqres is close to free and is where to start. fullres measures more accurately and costs real throughput, which on a hardware encoder is most of the reason you chose it.\n\nIt earns the most under a bitrate target, where the first pass tells the rate controller what it is about to spend. Under a fixed quality target there is much less for it to do.",
    "disabled|The default; a single pass.\nqres|Quarter-resolution first pass; the usual choice, and nearly free.\nfullres|Full-resolution first pass; the most accurate and the slowest."
  ],
  [
    "qmin",
    "",
    "-1 to 51 (default -1, no floor) - The best quantiser rate control may use; raise it to stop easy scenes soaking up bitrate",
    "Rate Control",
    "A floor on the quantiser, which is a ceiling on how many bits any one frame may be given. Lower quantisers mean higher quality, so this bounds how good the encode is allowed to get.\n\nRaising it stops easy scenes being encoded at a quality nobody will notice, which under a bitrate target frees those bits for the difficult material.\n\nThis is NVENC's own option rather than ffmpeg's generic qmin, and the two have different ranges. Measured against this build, a value of 55 is refused as out of range [-1 - 51], so it is the encoder's own bounds that apply here.\n\nUnder the quality target the tab emits, a tight floor can stop the encoder reaching the quality asked for, since it is no longer allowed to go where the target wants to take it.",
    "-1|The default; no floor.\n10|Stops easy scenes spending more than they need.\n15|A firmer cap, noticeably smaller files on easy content.\n20|A hard cap, for size-critical work on clean sources."
  ],
  [
    "qmax",
    "",
    "-1 to 51 (default -1, no ceiling) - The worst quantiser rate control may use; lower it to stop hard scenes collapsing",
    "Rate Control",
    "A ceiling on the quantiser, bounding how far quality is allowed to fall when the rate controller runs short of bits.\n\nLowering it prevents the blocky collapse that difficult scenes show at constrained bitrates. The consequence is that the encoder can no longer hit its bitrate target on those scenes and will overshoot instead - a quality floor and a size target cannot both be guaranteed.\n\nThe default of -1 is no limit, so this only matters where the bitrate target is tight. Like qmin it is NVENC's own option rather than ffmpeg's generic one, and it stops at 51.",
    "-1|The default; no ceiling.\n40|Prevents the worst collapse on hard scenes.\n35|A firm quality floor, at the cost of size predictability.\n51|The maximum, which is the same as no ceiling at all."
  ],
  [
    "init_qpI",
    "",
    "-1 to 51 (default -1, automatic) - Starting quantiser for I-frames; a seed for the rate controller, not a bound",
    "Rate Control",
    "The quantiser the rate controller starts I-frames at, before it has learnt anything about the source. It is a seed rather than a bound - once the encode is running the controller moves away from it - so the effect is concentrated in the opening seconds.\n\nThat is exactly where a bitrate-targeted encode looks worst. With nothing measured yet the controller guesses, and a bad guess shows as a soft or blocky first shot before it settles. Setting the three init_qp rows near where you know the encode will end up removes that.\n\nUnder rc constqp they are not a starting point at all: nothing adapts, so the initial values are the values used throughout.",
    "-1|The default; the driver picks.\n20|A high-quality start, for a generous bitrate.\n26|A middling start for ordinary delivery bitrates.\n32|A conservative start, for a tight bitrate."
  ],
  [
    "init_qpP",
    "",
    "-1 to 51 (default -1, automatic) - Starting quantiser for P-frames; normally a step or two above init_qpI",
    "Rate Control",
    "The P-frame half of init_qpI. P-frames predict from what came before, so they carry less new information and are conventionally coded a little coarser than the I-frames they hang off - a step or two above the I-frame value is the usual arrangement.\n\nSet it alongside init_qpI and init_qpB rather than on its own. The three together describe the shape of the GOP's bit distribution, and moving one without the others simply shifts where the error lands.\n\nUnder rc constqp this is not a starting point but the quantiser P-frames are actually coded at.",
    "-1|The default; the driver picks.\n22|About two steps above an init_qpI of 20, the conventional spacing.\n28|A tighter budget, with I-frames around 26.\n20|Equal to the I-frame value; spends more on P-frames than usual."
  ],
  [
    "init_qpB",
    "",
    "-1 to 51 (default -1, automatic) - Starting quantiser for B-frames; conventionally the coarsest of the three",
    "Rate Control",
    "The B-frame half of init_qpI. B-frames predict from both directions and nothing refers to them unless b_ref_mode says otherwise, so they are conventionally the coarsest of the three - a couple of steps above the P-frame value.\n\nIt does nothing where there are no B-frames, and NVENC produces none by default. The bf row is what creates them.\n\nWhere b_ref_mode is on, some B-frames are references after all, and coarsening those costs more than it saves - which is an argument for closing the gap to init_qpP rather than widening it.",
    "-1|The default; the driver picks.\n25|About three steps above an init_qpI of 20, the conventional spacing.\n22|A narrow spread, for use with b_ref_mode each or middle.\n30|Cheap B-frames, buying quality for the frames referencing them."
  ],
  [
    "g",
    "",
    "Frames between keyframes (default 12) - ffmpeg's GOP size; the tab sets none for NVENC, so its default applies",
    "Frames & Motion",
    "How many frames may pass before the encoder must start a new GOP with a keyframe. This is ffmpeg's own option rather than one of NVENC's, and it is the only way to reach the keyframe interval from this tab: the tab does not emit a -g for NVENC as it does for the software encoders, so what runs is ffmpeg's own default of 12.\n\nTwelve frames is very short - half a second at 25 fps - and a keyframe is the most expensive frame there is. Raising it is one of the larger size savings available from this tab, and it costs nothing but seek granularity.\n\nSet it against how the file will be used. A couple of seconds is normal for delivery, a segmented stream wants the segment length, and an editing intermediate wants it low or at 1 for all-intra.\n\nA scene cut can still force a keyframe of its own where the lookahead is running and no-scenecut is not set, so this is a ceiling on the spacing rather than a fixed grid.",
    "12|ffmpeg's default, and what runs if this row is left blank.\n250|Ten seconds at 25 fps; a sound choice for a delivery file.\n50|Two seconds at 25 fps, for tighter seeking and streaming.\n1|Every frame a keyframe, for an editing intermediate."
  ],
  [
    "bf",
    "",
    "0 or more, -1 for automatic (default 0) - B-frames between reference frames; NVENC makes none unless asked",
    "Frames & Motion",
    "The longest run of consecutive B-frames the encoder may insert. B-frames predict from both earlier and later frames, which makes them by far the cheapest frames to code, so allowing them lets static and slow material spend its bits where something is actually happening.\n\nThis is ffmpeg's own option and its default is 0, so an NVENC encode produces no B-frames at all unless this row is filled in. That is a real compression difference on its own, and it is also what leaves b_adapt, b_ref_mode, init_qpB, unidir_b and tf_level doing nothing on an otherwise default command line.\n\nThe driver clamps the run to what the GPU implements and ffmpeg reports it, saying \"B-frames=N is not supported by this GPU, switched to M\". HEVC B-frames arrived on NVENC later than H.264's, so a card that produces them happily on the other encoder may produce none here.\n\nEvery B-frame adds encoding latency and a frame of decode delay, which is why a low-latency tune wants this at 0.",
    "0|ffmpeg's default; no B-frames at all.\n3|A sound general setting for live action.\n4|The minimum tf_level requires, and where most GPUs top out.\n-1|Automatic; leaves the choice to the encoder."
  ],
  [
    "refs",
    "",
    "1 or more (default 1) - Reference frames the motion search may use; more helps repetitive material",
    "Frames & Motion",
    "How many previously decoded frames the motion search may predict from. More references help whenever the same content reappears - repetitive motion, cuts back to an earlier shot, animation that reuses cels - because the encoder can point at the frame that actually matches.\n\nThis is ffmpeg's own option, and its default of 1 is far lower than a software encoder's. Raising it is cheap on hardware, where the search is fixed-function rather than something paid for in encoding time.\n\nThe GPU decides whether it can oblige: ffmpeg prints \"Multiple reference frames are not supported by the device\" and carries on with one where it cannot.\n\nThe level bounds it as well, through the size of the decoded picture buffer, and that bound tightens as the picture gets larger - so a count that is legal at 1080p need not be at 4K. Past it, the file no longer matches the level in its own header and a compliant decoder is entitled to refuse it.",
    "1|ffmpeg's default; a single reference.\n2|A cheap improvement wherever the GPU supports it.\n4|A sound ceiling for delivery, well inside what the levels allow.\n8|Repetitive or animated material, at some compatibility risk."
  ],
  [
    "b_adapt",
    "",
    "0 or 1 (default 1) - Whether B-frame placement is decided per scene; only does anything with rc-lookahead set",
    "Frames & Motion",
    "Decides where in the sequence B-frames actually go, up to the limit bf sets. On, the encoder uses the lookahead to place them where they pay; off, it simply always uses the maximum run.\n\nffmpeg's own description of this option is explicit that it applies when lookahead is enabled, so with rc-lookahead at 0 the setting is inert whichever way it is left.\n\nTurning it off is a latency and determinism choice rather than a quality one: it removes a decision that depends on frames the encoder has not yet delivered. For a file there is no reason to.",
    "1|The default; B-frames placed where the lookahead says they pay.\n0|A fixed maximum run, for deterministic low-latency output."
  ],
  [
    "b_ref_mode",
    "",
    "disabled, each or middle (default -1, left to the driver) - Whether B-frames may themselves be used as references",
    "Frames & Motion",
    "Normally B-frames are never referenced by anything, so they are disposable. This allows some of them to act as references for other frames, which builds a hierarchy and improves compression noticeably on material with long B-frame runs.\n\neach makes every B-frame a reference. middle promotes only the middle of each run, which is the arrangement a software encoder's B-pyramid produces and the cheaper of the two. disabled states the conventional behaviour outright, rather than leaving the field unset the way the default does.\n\nIt is a feature of the later NVENC generations, and ffmpeg names which part is missing rather than failing: \"Each B frame reference mode is not supported\", or \"Middle B frame reference mode is not supported\".\n\nIt needs B-frames to exist, so the bf row comes first. And a referenced B-frame is no longer disposable, which is an argument against coarsening it with init_qpB.",
    "middle|Promotes the middle B-frame of each run; the usual choice.\neach|Every B-frame a reference; the strongest form.\ndisabled|Conventional disposable B-frames, stated explicitly.\n-1|The default; the field is left unset for the driver to decide."
  ],
  [
    "unidir_b",
    "",
    "0 or 1 (default 0) - Lets B-frames predict in one direction only, for latency rather than compression",
    "Frames & Motion",
    "A B-frame normally predicts in both directions, from a frame before it and a frame after it, and that backward reference is most of why it codes so cheaply. This restricts them to looking backwards only, keeping the other half of what a B-frame is - that nothing refers to it, so it can be coded cheaply without the cost carrying forward.\n\nWhat it buys is the reordering. A frame that predicts from something later cannot be coded until that later frame has arrived, which is the delay B-frames impose on a live encode; one that only looks back does not have to wait. It is a low-latency feature, and it belongs with tune ll or ull rather than with an offline encode.\n\nFor a file there is little reason to set it. Giving up the backward reference gives up most of the compression B-frames were there for, and an offline encode has no latency budget to spend the difference on.\n\nIt needs B-frames to exist at all, so the bf row comes first, and it needs a GPU that implements it - ffmpeg says \"Unidirectional B-Frames not supported by the device\" and carries on without it. The H.264 encoder has no such option; this is one of the three that are HEVC-only here.",
    "0|The default; B-frames predict in both directions.\n1|Unidirectional B-frames, for low-latency encoding."
  ],
  [
    "no-scenecut",
    "",
    "0 or 1 (default 0) - Set to 1 to stop the lookahead inserting an I-frame at a scene cut",
    "Frames & Motion",
    "With the lookahead running, NVENC inserts an I-frame where it detects a cut, because a cut is the one place a keyframe is nearly free and predicting across it is nearly useless. This turns that off.\n\nffmpeg's own description says it applies when lookahead is enabled, so like b_adapt it does nothing while rc-lookahead is 0.\n\nThe reason to set it is a fixed keyframe grid - segmented streaming, or a workflow that cuts on a known frame count - where a keyframe arriving early is a problem rather than a saving. For an ordinary file leave it alone: adaptive I-frames are one of the larger wins the lookahead buys.",
    "0|The default; a detected cut gets its own I-frame.\n1|Keyframes only on the g interval, for a fixed segment grid."
  ],
  [
    "forced-idr",
    "",
    "0 or 1 (default 0) - Makes a forced keyframe an IDR, so it is a real seek point",
    "Frames & Motion",
    "A keyframe is not automatically a point a decoder can start from. An I-frame refreshes the picture; an IDR additionally empties the reference list, so nothing after it may refer to anything before it. Only the IDR is a clean entry point.\n\nThis makes every keyframe forced from outside - by a -force_key_frames expression, or by a segmenting muxer asking for one - an IDR rather than a plain I-frame.\n\nSet it whenever the output is going to be cut, segmented or seeked at those points. It costs a little compression, since the frames after an IDR lose access to references they could otherwise have used.",
    "0|The default; forced keyframes are plain I-frames.\n1|Forced keyframes become IDRs, for segmenting and clean seeking."
  ],
  [
    "nonref_p",
    "",
    "0 or 1 (default 0) - Lets the encoder mark some P-frames non-reference, so it can code them cheaply",
    "Frames & Motion",
    "A P-frame is normally a reference, so its errors propagate to everything predicting from it and the encoder must spend accordingly. This lets it mark some of them as referenced by nothing, which means they can be coded cheaply without the cost carrying forward.\n\nIt is the B-frame trick applied to P-frames, and it is a way to get some of that benefit where B-frames are unavailable - a GPU with no HEVC B-frame support, or a latency budget that cannot afford the reordering.\n\nInsertion is automatic and there is no count to set. It helps most under a tight bitrate, where the saved bits go somewhere useful, and least where the bitrate is generous.",
    "0|The default; every P-frame is a reference.\n1|Automatic non-reference P-frames, for tight bitrates and low latency."
  ],
  [
    "weighted_pred",
    "",
    "0 or 1 (default 0) - Weighted prediction, letting a frame scale its reference; helps fades and flashes",
    "Frames & Motion",
    "Weighted prediction lets a frame scale and offset the reference it predicts from, so a frame that is simply brighter or darker than the one before does not have to be re-coded from scratch. Fades, flashes and lighting changes are where it earns its keep.\n\nNVENC's version is a plain on/off where a software encoder offers a choice of strengths, and the GPU may not implement it at all - which ffmpeg reports as \"Weighted Prediction not supported\" before carrying on without it.\n\nIt is not free of side effects on this encoder. ffmpeg also carries the message \"Split encoding not supported with weighted prediction enabled\", so on the multi-engine cards this gives up the parallelism the split_encode_mode row is about. On a source with no fades in it there is nothing to gain in exchange.",
    "0|The default, off.\n1|On, for sources with fades, flashes or changing light."
  ],
  [
    "profile",
    "",
    "main, main10, rext or mv (default main) - Caps the tools used so limited decoders can play the file",
    "Stream & Compatibility",
    "Restricts the encoder to the coding tools and bit depths a given HEVC profile permits, so a decoder implementing only that profile can play the result. main is 8-bit 4:2:0 and is the default here; main10 is 10-bit 4:2:0; rext is the Range Extensions profile, which is where 4:4:4 chroma lives.\n\nmain10 is the one worth knowing about. Ten-bit HEVC bands far less than eight-bit and usually compresses a little better, and unlike H.264's high10 it is what streaming services and HDR already use, so current hardware decodes it. The bit depth itself comes from the colour format the tab sets, or from the highbitdepth row; this names the profile the stream is tagged with, and the two have to agree.\n\nThat is the trap. The profile and the colour format are set in different places, and asking for main here over a ten-bit colour format is a contradiction the driver resolves rather than a setting that takes effect. Change them together.\n\nmv is Multiview HEVC, for a stereoscopic stream carrying two views. It is not a way to encode an ordinary file - ffmpeg carries both \"Multiview not supported by the device\" and \"Multiview encoding only works for Main profile content\", and nothing on this tab produces a second view to put in it. And the row is named profile:v rather than plain profile because ffmpeg has this option on audio encoders too: a bare -profile draws \"Please use -profile:a or -profile:v, -profile is ambiguous\" and is then applied to whatever streams will take it, which is not a thing to leave to chance in a command that also carries an audio encoder.",
    "main|The default; 8-bit 4:2:0 and the broadest compatibility.\nmain10|10-bit 4:2:0; better gradients, and widely supported by hardware.\nrext|Range Extensions, for a 4:4:4 colour format.\nmv|Multiview HEVC; a stereoscopic profile, not an ordinary one."
  ],
  [
    "tier",
    "",
    "main or high (default main) - The HEVC tier, which raises the bitrate and buffer limits the level allows",
    "Stream & Compatibility",
    "HEVC splits every level into two tiers. Main tier is sized for consumer delivery; high tier raises the bitrate and buffer ceilings that same level permits, several times over, for contribution and mastering work. It exists so that a level number does not have to mean one bitrate for a Blu-ray and for a studio master alike.\n\nIt changes no coding decision on its own. What it changes is what the stream's own header says it is allowed to do, and therefore which decoders will accept it - a decoder that claims main tier at your level is entitled to refuse a file tagged high tier, so setting it for no reason is a way to have a file rejected that would otherwise have played.\n\nIt only means anything alongside the level row, since a tier qualifies a level, and high tier is defined from level 4 upwards. Set it where a genuinely high bitrate would otherwise exceed what your level permits, and leave it alone otherwise.\n\nThis is one of the three options the HEVC encoder has and the H.264 one does not - H.264 has no tiers at all.",
    "main|The default; consumer delivery, and what hardware decoders expect.\nhigh|Contribution and mastering bitrates, from level 4 upwards."
  ],
  [
    "level",
    "",
    "auto, or 1 to 6.2 (default auto) - Decoder level written to the stream header",
    "Stream & Compatibility",
    "The decoder level written into the stream header. Levels bound resolution, frame rate, bitrate and how many pictures may be held in the decoded picture buffer, and hardware decoders advertise the highest level they implement.\n\nThe encoder works out a suitable level on its own, so this is only worth setting when a target demands a specific one - a television or a set-top box that refuses anything above 5.1, say. The tier row qualifies whatever is set here, main and high tier at the same level allowing very different bitrates.\n\nSpell it exactly as the list has it. These names are constants, and a spelling that is not one of them is not rejected: it is evaluated as a plain number instead, and measured against this build only a result outside 0 to 186 is refused. So 6.3 is accepted and tags the stream with a level nobody asked for, while H.264's 1b - which is not an HEVC level - is refused outright.",
    "auto|The default; the encoder picks a level to fit.\n4.1|A common hardware ceiling for 1080p.\n5.1|4K delivery to modern decoders.\n6.2|The highest defined; 8K and high frame rates."
  ],
  [
    "aud",
    "",
    "0 or 1 (default 0) - Writes access unit delimiters, which some transport and broadcast chains require",
    "Stream & Compatibility",
    "An access unit delimiter is a small NAL unit marking where each coded frame begins. It carries no picture data - it exists so a demuxer can find frame boundaries without parsing the slices.\n\nAlmost nothing needs it in a file. MPEG-TS and broadcast-style workflows are where it comes up, along with some hardware decoders and analysis tools that expect the boundary marked explicitly.\n\nThe cost is a handful of bytes per frame, so setting it where it is not needed is harmless but pointless.",
    "0|The default; no delimiters.\n1|Writes them, for MPEG-TS and broadcast delivery."
  ],
  [
    "s12m_tc",
    "",
    "0 or 1 (default 1) - Carries SMPTE 12M timecode from the source into the encoded stream",
    "Stream & Compatibility",
    "SMPTE 12M timecode, written into the video as a time code SEI message beside the picture rather than as a track of its own. Where the source carries one - a camera original, a broadcast capture, anything that came off tape - this passes it through, and like a53cc beside it, it is on by default.\n\nThe reverse case is what makes the row worth knowing. A timecode that survives a re-encode is this option's doing and has nothing to do with any timecode the container carries, so a file that looks as though it has no metadata at all can still arrive with one embedded in the video, where an editor will find it and a player will not.\n\nTurn it off for a clean master, or where something downstream reads the SEI and gets confused by timing from the original. It costs a few bytes a frame when there is a timecode to write and nothing at all when there is not.",
    "1|The default; a timecode in the source is carried through.\n0|Strips the timecode SEI from the output."
  ],
  [
    "a53cc",
    "",
    "0 or 1 (default 1) - Carries A/53 closed captions from the source into the encoded stream",
    "Stream & Compatibility",
    "Broadcast and capture sources carry closed captions inside the video itself, as A/53 caption data attached to the picture rather than as a subtitle track. This passes that data through into the encoded stream, and unusually for this list it is on by default.\n\nThe reason to know the row is here is the reverse case. A re-encode of a US broadcast capture keeps its captions through this option and nothing on the subtitle side is involved, which is how they survive a conversion that appears to carry no subtitle track at all.\n\nTurn it off to strip them - for a clean master, or where something downstream trips over caption data it was not expecting.",
    "1|The default; captions in the source are carried through.\n0|Strips the caption data from the output."
  ]
]
